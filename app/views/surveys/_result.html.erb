<div class="display-dog flex justify-center">
  <div class="bg-gray-500" style="width:300px;height:300px;"></div>
</div>
<div class="overflow-hidden bg-white shadow sm:rounded-lg">
  <div class="px-4 py-5 sm:px-6">
    <h3 class="text-2xl text-center font-bold leading-6 text-gray-900"><%= breed.name %></h3>
  </div>
  <div class="border-t border-gray-200">
    <dl>
      <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Description</dt>
        <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= breed.description %></dd>
      </div>
      <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Family Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.family_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Children Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.children_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Other Dog Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.other_dog_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Shedding Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.shedding_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Grooming Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.grooming_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Drooling Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.drooling_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Stranger Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.stranger_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Playfulness Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.playfulness_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Protective Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.protective_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Adaptability Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.adaptability_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Trainability Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.trainability_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Energy Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.energy_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Barking Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.barking_score, i) %>
          <% end %>
        </div>
      </div>
      <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
        <dt class="text-sm font-medium text-gray-500">Mental Stimulation Score</dt>
        <div class="star-values flex my-4">
          <% (1..5).each do |i| %>
            <%= display_stars(breed.mental_stim_score, i) %>
          <% end %>
        </div>
      </div>
    </dl>
  </div>
</div>